/*
    Array methods practice (pairs).
        - Create an array that contains two of your favorite things to do.
        - Add another thing to that list using an array method.
        - Sort the array.
        - Reverse the order of the array.
*/

let firstArr = ["workout", "Hunting"]

console.log(firstArr)

firstArr.push("Drawing")

console.log(firstArr, "<--- Adding")

firstArr.sort()

console.log(firstArr, "<--- Sorting")

console.log(firstArr.reverse(), "<--- Reversing")