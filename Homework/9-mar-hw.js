const demonSlayer = {
    damage: 10,
    health: 100
}


const theDemon = {
    health: 200,
    damage: Math.floor(Math.random()*100)
}


const slayerAttacks = {
    dropKick: ("Drop Kick ", (Math.round(Math.random()*25),
    falconPunch: ("Incoming Falcon punch", (Math.round(Math.random()*50),
    superSmash: ("SUPER SMASH", (Math.round(Math.random()*90)
} 

function timeToFight()

}
